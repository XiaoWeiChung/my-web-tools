## 1. 项目骨架

- [x] 1.1 创建 `CoconutEnglish/index.html`，含首页/主题/单词/字母/游戏五个 `.screen` 区块及声音开关按钮
- [x] 1.2 创建 `CoconutEnglish/style.css`，定义椰子风格的明亮护眼配色、大按钮、卡片网格与基础动画
- [x] 1.3 创建 `CoconutEnglish/data.js`，定义主题、单词（含图标 key/英文/中文）、A-Z 字母及代表词数据
- [x] 1.4 实现 `getIcon(key)` 图标查找函数（独立 `icons.js`），返回内联 SVG 卡通插画字符串（可爱风、圆润描边），覆盖核心词表

## 2. 发音与声音开关（speech-playback）

- [x] 2.1 在 `app.js` 实现 `speak(text)`：判断声音开关、`cancel()` 防排队、选英文嗓音、设较慢语速
- [x] 2.2 实现全局声音开关状态与按钮图标切换（🔊/🔇）
- [x] 2.3 探测 `speechSynthesis` 是否可用，不支持时静默降级不报错

## 3. 单词主题学习（word-learning）

- [x] 3.1 渲染主题选择网格，点击进入对应主题
- [x] 3.2 渲染单词卡网格（大卡通图标 + 英文 + 中文），点击朗读并播放视觉反馈
- [x] 3.3 实现主题页与单词页的返回导航

## 4. 字母 ABC（alphabet-learning）

- [x] 4.1 渲染 A-Z 26 张字母卡（大写字母 + 代表词图标）
- [x] 4.2 点击字母卡朗读字母名称与代表单词，并播放视觉反馈

## 5. 听音找图游戏（listening-game）

- [x] 5.1 实现出题逻辑：随机目标词 + 同主题干扰项，打乱排列后渲染图片卡
- [x] 5.2 实现"再听一次"按钮重播目标词
- [x] 5.3 实现作答反馈：答对加分+鼓励动画并进入下一题；答错温柔提示且不扣分、可重试
- [x] 5.4 在游戏界面持续显示累计得分

## 6. 休息提醒（break-reminder）

- [x] 6.1 实现前台活跃时长累计计时器，监听 `visibilitychange` 在隐藏时暂停、可见时继续
- [x] 6.2 满 20 分钟弹出休息提醒覆盖层（含卡通插画与提示文字），并暂停当前语音
- [x] 6.3 点击确认关闭弹框、重置计时并继续下一轮

## 7. 验收

- [x] 7.1 代码层校验：图标/数据完整（42 个图标全覆盖、无缺失）、JS 语法通过；逐模块场景待浏览器人工确认
- [x] 7.2 验证声音关闭时仅视觉反馈、无声音；不支持语音时 try/catch 静默降级
- [x] 7.3 图标为内联 SVG 矢量，放大/高分屏清晰不糊
- [ ] 7.4 验证休息提醒在累计 20 分钟后弹出、切后台暂停计时（需浏览器人工确认）
- [ ] 7.5 验证移动端/触屏点击与响应式布局（需浏览器人工确认）
